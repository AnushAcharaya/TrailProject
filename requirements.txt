# Core Django
Django>=5.2

# Django REST Framework
djangorestframework

# JWT Authentication
djangorestframework-simplejwt

# PostgreSQL
psycopg2

# Environment Variables
python-dotenv

# Image & File Upload
Pillow

# API Documentation (Swagger)
drf-yasg

# CORS (for frontend React / mobile apps)
django-cors-headers

# Celery for async tasks (emails, SMS, notifications)
celery
redis

# Optional — For better password hashing
# argon2-cffi

# Django Filters (required by medical app)
django-filter

# WebSocket Support (Django Channels)
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0

# Google OAuth (ID token verification)
google-auth>=2.30.0

# DNS MX lookup (used to validate that an email's domain is hosted by Google)
dnspython>=2.6.0
